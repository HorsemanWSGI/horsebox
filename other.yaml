imports: !include:test.yaml

workers:
  test: !new:multiprocessing.Process
    target: !name:print
      - this is a worker
  forever: !new:multiprocessing.Process
    target: !name:horsebox.builtins.asyncio_loop
      - !apply:asyncio.new_event_loop

runner: !ref <imports[runner]>
